<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.back.mapper.EnrollMapper">

    <select id="pageQuery" resultType="com.example.back.vo.EnrollVO">
        SELECT
        e.id,
        e.user_id,
        u.username,
        e.exam_id,
        ex.exam_name AS examName,
        e.status,
        e.enroll_time
        FROM enroll e
        LEFT JOIN db_user u ON e.user_id = u.id
        LEFT JOIN exam ex ON e.exam_id = ex.id
        <where>
            <if test="dto.userId != null">
                e.user_id = #{dto.userId}
            </if>
            <if test="dto.examId != null">
                AND e.exam_id = #{dto.examId}
            </if>
        </where>
    </select>


</mapper>
